# ============================================================
# Funda Tracker — Home Assistant package (OPTIONAL)
# ============================================================
# /config/packages/funda.yaml
#
# This package is OPTIONAL. Core sensors (value, bounds, stats,
# confidence, price/m²) are pushed directly by the add-on.
#
# This package adds:
#   - Finance input helpers (purchase price, investment, mortgage)
#   - Finance template sensors (overwaarde, winst, ROI)
#   - Automations (notifications, alerts, yearly summary)
# ============================================================

# --- Input helpers ---
#
# FINANCE HELPERS — Set these in Settings → Helpers:
#
#   Aankoopprijs:       What you paid for the house (koopsom).
#   Totale Investering: Purchase + renovation + loans + cash.
#   Hypotheek:          Outstanding mortgage balance.
#
# Example: Bought for €350k, renovated €150k total = €500k invested.
#
input_number:
  funda_purchase_price:
    name: "Funda Aankoopprijs"
    icon: mdi:cash
    min: 0
    max: 5000000
    step: 1000
    unit_of_measurement: "€"
    mode: box

  funda_total_investment:
    name: "Funda Totale Investering"
    icon: mdi:cash-multiple
    min: 0
    max: 5000000
    step: 1000
    unit_of_measurement: "€"
    mode: box

  funda_mortgage_balance:
    name: "Funda Hypotheek Restschuld"
    icon: mdi:bank
    min: 0
    max: 5000000
    step: 1000
    unit_of_measurement: "€"
    mode: box

  funda_value_alert_high:
    name: "Funda Bovengrens Alert"
    icon: mdi:arrow-up-bold-circle
    min: 0
    max: 5000000
    step: 5000
    unit_of_measurement: "€"
    mode: box

  funda_value_alert_low:
    name: "Funda Ondergrens Alert"
    icon: mdi:arrow-down-bold-circle
    min: 0
    max: 5000000
    step: 5000
    unit_of_measurement: "€"
    mode: box

# --- Finance template sensors ---
template:
  - sensor:
      - name: "Funda Overwaarde"
        unique_id: funda_equity
        state: >
          {% set value = states('sensor.funda_house_value') | int(0) %}
          {% set mortgage = states('input_number.funda_mortgage_balance') | int(0) %}
          {{ value - mortgage }}
        unit_of_measurement: "€"
        icon: mdi:piggy-bank
        availability: >
          {{ states('input_number.funda_mortgage_balance') | int(0) > 0
             and states('sensor.funda_house_value') not in ['unknown', 'unavailable'] }}

      - name: "Funda Marktwinst"
        unique_id: funda_market_profit
        state: >
          {% set value = states('sensor.funda_house_value') | int(0) %}
          {% set purchase = states('input_number.funda_purchase_price') | int(0) %}
          {{ value - purchase }}
        unit_of_measurement: "€"
        icon: >
          {% set value = states('sensor.funda_house_value') | int(0) %}
          {% set purchase = states('input_number.funda_purchase_price') | int(0) %}
          {{ 'mdi:trending-up' if value >= purchase else 'mdi:trending-down' }}
        availability: >
          {{ states('input_number.funda_purchase_price') | int(0) > 0
             and states('sensor.funda_house_value') not in ['unknown', 'unavailable'] }}

      - name: "Funda Markt ROI"
        unique_id: funda_market_roi
        state: >
          {% set value = states('sensor.funda_house_value') | int(0) %}
          {% set purchase = states('input_number.funda_purchase_price') | int(0) %}
          {{ ((value - purchase) / purchase * 100) | round(1) if purchase > 0 else 0 }}
        unit_of_measurement: "%"
        icon: mdi:chart-line
        availability: >
          {{ states('input_number.funda_purchase_price') | int(0) > 0
             and states('sensor.funda_house_value') not in ['unknown', 'unavailable'] }}

      - name: "Funda Totale Winst"
        unique_id: funda_total_profit
        state: >
          {% set value = states('sensor.funda_house_value') | int(0) %}
          {% set invested = states('input_number.funda_total_investment') | int(0) %}
          {{ value - invested }}
        unit_of_measurement: "€"
        icon: >
          {% set value = states('sensor.funda_house_value') | int(0) %}
          {% set invested = states('input_number.funda_total_investment') | int(0) %}
          {{ 'mdi:trending-up' if value >= invested else 'mdi:trending-down' }}
        availability: >
          {{ states('input_number.funda_total_investment') | int(0) > 0
             and states('sensor.funda_house_value') not in ['unknown', 'unavailable'] }}

      - name: "Funda Totale ROI"
        unique_id: funda_total_roi
        state: >
          {% set value = states('sensor.funda_house_value') | int(0) %}
          {% set invested = states('input_number.funda_total_investment') | int(0) %}
          {{ ((value - invested) / invested * 100) | round(1) if invested > 0 else 0 }}
        unit_of_measurement: "%"
        icon: mdi:chart-areaspline
        availability: >
          {{ states('input_number.funda_total_investment') | int(0) > 0
             and states('sensor.funda_house_value') not in ['unknown', 'unavailable'] }}

# --- Automations ---
automation:
  - alias: "Funda waarde update notificatie"
    id: funda_value_update_notification
    trigger:
      - platform: state
        entity_id: sensor.funda_house_value
    condition:
      - condition: template
        value_template: >
          {{ trigger.from_state.state not in ['unknown', 'unavailable']
             and trigger.from_state.state != trigger.to_state.state }}
    action:
      - service: notify.notify
        data:
          title: "🏠 Funda Woningwaarde Update"
          message: >
            Nieuwe waarde: €{{ '{:,}'.format(states('sensor.funda_house_value') | int) }}
            {% if states('sensor.funda_maandwijziging') not in ['unknown', 'unavailable', '0'] %}
            Maandwijziging: €{{ states('sensor.funda_maandwijziging') }} ({{ states('sensor.funda_maandwijziging_pct') }}%)
            {% endif %}
    mode: single

  - alias: "Funda waarde bovengrens alert"
    id: funda_threshold_high_alert
    trigger:
      - platform: template
        value_template: >
          {{ states('sensor.funda_house_value') | int(0) >= states('input_number.funda_value_alert_high') | int(0)
             and states('input_number.funda_value_alert_high') | int(0) > 0 }}
    action:
      - service: notify.notify
        data:
          title: "🏠📈 Woningwaarde boven doelgrens!"
          message: >
            Je woning is nu €{{ '{:,}'.format(states('sensor.funda_house_value') | int) }} waard.
    mode: single

  - alias: "Funda waarde ondergrens alert"
    id: funda_threshold_low_alert
    trigger:
      - platform: template
        value_template: >
          {{ states('sensor.funda_house_value') | int(0) <= states('input_number.funda_value_alert_low') | int(0)
             and states('input_number.funda_value_alert_low') | int(0) > 0 }}
    action:
      - service: notify.notify
        data:
          title: "🏠📉 Woningwaarde onder doelgrens!"
          message: >
            Je woning is nu €{{ '{:,}'.format(states('sensor.funda_house_value') | int) }} waard.
    mode: single

  - alias: "Funda significante wijziging alert"
    id: funda_significant_change_alert
    trigger:
      - platform: state
        entity_id: sensor.funda_house_value
    condition:
      - condition: template
        value_template: >
          {% set pct = states('sensor.funda_maandwijziging_pct') | float(0) %}
          {{ pct > 2.0 or pct < -2.0 }}
    action:
      - service: notify.notify
        data:
          title: "🏠⚠️ Ongewone waardeverandering!"
          message: >
            Je woningwaarde is veranderd met {{ states('sensor.funda_maandwijziging_pct') }}% — dat is meer dan normaal.
    mode: single

  - alias: "Funda jaarsamenvatting"
    id: funda_yearly_summary
    trigger:
      - platform: time
        at: "10:00:00"
    condition:
      - condition: template
        value_template: "{{ now().month == 1 and now().day == 1 }}"
    action:
      - service: notify.notify
        data:
          title: "🏠🎉 Jaarsamenvatting Woningwaarde"
          message: >
            Huidige waarde: €{{ '{:,}'.format(states('sensor.funda_house_value') | int) }}
            {% if states('sensor.funda_jaarwijziging') not in ['unknown', 'unavailable'] %}
            Jaarwijziging: €{{ states('sensor.funda_jaarwijziging') }} ({{ states('sensor.funda_jaarwijziging_pct') }}%)
            {% endif %}
            {% if states('input_number.funda_purchase_price') | int(0) > 0 %}
            Marktwinst: €{{ states('sensor.funda_marktwinst') }} ({{ states('sensor.funda_markt_roi') }}%)
            {% endif %}
            {% if states('input_number.funda_total_investment') | int(0) > 0 %}
            Totale winst: €{{ states('sensor.funda_totale_winst') }} ({{ states('sensor.funda_totale_roi') }}%)
            {% endif %}
    mode: single
